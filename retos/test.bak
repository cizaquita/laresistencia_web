<?php
    
    //Connect to the database
    $host = "localhost";
    $user = "retos";                     //Your Cloud 9 username
    $pass = "!ret0s_";                                  //Remember, there is NO password by default!
    $db = "rescolretos_db";                                  //Your database name you want to connect to
    $port = 3306;                                //The port #. It is always 3306
    
    $connection = mysqli_connect($host, $user, $pass, $db, $port)or die(mysql_error());



    //And now to perform a simple query to make sure it's working
    $query = "SELECT * FROM users";
    $result = mysqli_query($connection, $query);

    while ($row = mysqli_fetch_assoc($result)) {
        echo "test: " . $host = getenv('IP') . "The ID is: " . $row['id'] . " and the Username is: " . $row['username'];
    }

?>